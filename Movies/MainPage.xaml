<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Movies.MainPage"
             xmlns:local="clr-namespace:Movies.Controls"
             xmlns:binding="clr-namespace:Movies.ViewModels"
             BackgroundColor="DarkGray"
             >

    <ContentPage.BindingContext>
        <binding:MoviesViewModel />
    </ContentPage.BindingContext>

    <!--<StackLayout>
        <Label Text="{Binding Value}" />
        <local:RatingView Value="5" Color="Red" />
    </StackLayout>-->
    <CollectionView ItemsSource="{Binding Movies}">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Frame BorderColor="Gray" Margin="40" BackgroundColor="Gray" HasShadow="True" CornerRadius="10">
                    <Grid BackgroundColor="LightGray">
                        <Image Source="{Binding Image}" Aspect="AspectFill"/>
                        <local:RatingView Value="{Binding Rating}" Color="{Binding Color}" VerticalOptions="End" HeightRequest="50" BackgroundColor="LightGray"/>
                    </Grid>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>

</ContentPage>
